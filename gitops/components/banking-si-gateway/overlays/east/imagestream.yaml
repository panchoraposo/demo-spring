apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: api-gateway
  namespace: banking-si-apps
spec:
  # Off: Deployment pulls Quay via quay-pull; local lookup rewrites break recover.
  lookupPolicy:
    local: false

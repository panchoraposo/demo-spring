apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: api-gateway
  labels:
    app.kubernetes.io/name: api-gateway
    # Per-cluster OpenShift Route entry for the SI demo (east / west).
    route-type: si-cluster
spec:
  # Patched per overlay (si-gateway.<apps-domain>).
  host: si-gateway.apps.example.com
  to:
    kind: Service
    name: api-gateway
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect


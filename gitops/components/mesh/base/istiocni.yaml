apiVersion: sailoperator.io/v1
kind: IstioCNI
metadata:
  name: default
  namespace: istio-cni
  labels:
    app.kubernetes.io/part-of: banking-demo
spec:
  # OSSM 3.4 / Sail — pin when catalog exposes an explicit patch.
  version: v1.30-latest
  profile: ambient
  values:
    cni:
      ambient:
        enabled: true
        dnsCapture: true

apiVersion: v1
kind: Service
metadata:
  name: banking-service
  labels:
    istio.io/global: "true"
    istio.io/use-waypoint: banking-service-waypoint
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: banking-service

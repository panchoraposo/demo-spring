apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - servicemonitor.yaml
  - trigger-authentication.yaml
  - scaledobject.yaml
  # Credentials: ExternalSecret banking-service-db

commonLabels:
  app.kubernetes.io/name: banking-service
  app.kubernetes.io/part-of: banking-demo
  app.kubernetes.io/component: backend

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - subscription-kiali.yaml
  - namespace.yaml
  - kiali.yaml
  # Remote cluster kubeconfig Secrets (kiali.io/multiCluster=true) are created by
  # scripts/mesh/sync-kiali-multicluster-secrets.sh — not stored in Git.

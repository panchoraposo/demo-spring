apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - imagestream.yaml
  - scc-anyuid.yaml
  # Chart pre-install Secret hooks are incompatible with Argo CD (deleted after PreSync).
  # Secrets + rendered conjur-oss chart (helm template --skip-tests, hooks stripped).
  - helm-secrets.yaml
  - rendered-release.yaml
  - route.yaml

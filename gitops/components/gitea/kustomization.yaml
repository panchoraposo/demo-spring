apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespace + public Route. Gitea pods come from the Helm Application
# (or scripts/bootstrap-gitea.sh install).
resources:
  - namespace.yaml
  - route.yaml
  - scc-anyuid.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RHTPA operand + RHDA backend for Dev Spaces Dependency Analytics.
# Apply secrets (OIDC/DB) before the TrustedProfileAnalyzer CR is Ready — see README.
resources:
  - objectbucketclaim.yaml
  - postgresql.yaml
  - rhda-backend.yaml
  - trustedprofileanalyzer.yaml

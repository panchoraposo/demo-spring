apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Spoke ExternalSecrets (postgresql / banking-service).
# Jenkins + github-ci secrets live in external-secrets-hub on acm.
resources:
  - namespaces.yaml
  - clustersecretstore-conjur.yaml
  - externalsecret-postgresql.yaml
  - externalsecret-banking-service.yaml


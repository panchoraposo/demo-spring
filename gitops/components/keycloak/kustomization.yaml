apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - keycloak-db.yaml
  - keycloak.yaml
  - realm-import-job.yaml
  - route.yaml
  # Credentials: ExternalSecrets keycloak-db-secret / keycloak-admin


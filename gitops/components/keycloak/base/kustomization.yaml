apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - externalsecrets.yaml
  - keycloak-db.yaml
  - keycloak.yaml
  - realm-import-job.yaml
  # trustify realm is owned by Application trustify-realm (TPA path), not here.
  - route.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Hub (acm): ESO for Conjur→Jenkins secrets; GitOps for drift control.
resources:
  - namespace-eso.yaml
  - subscription-eso.yaml
  - subscription-openshift-gitops.yaml

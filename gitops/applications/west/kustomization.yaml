apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Spoke west — mirror of east; independent PostgreSQL + Keycloak.
resources:
  # wave 0
  - platform-operators.yaml
  # wave 1
  - eso-operand.yaml
  # wave 2
  - mesh.yaml
  - devspaces.yaml
  # wave 3
  - external-secrets-config.yaml
  # wave 4+
  - postgresql.yaml
  - keycloak.yaml
  - banking-service.yaml
  - api-gateway.yaml
